package designPattern.chp5_behavior_patterns;

/**
 * 访问者
 *
 * 1 意图  表示一个作用于某对象结构中的 各元素 操作。它使你可以在不改变各元素的类的前提下定义作用于这些元素的新操作。
 *
 * 这些操作要求对不同的节点进行不同处理。但是针对其中的一种类型来说，相关的操作一致。
 * 通过这种方式可以实现 多种模式。将每种模式相关功能统一抽离出来；
 *
 *  相关实现问题
 *  1 双分派（double-dispatch）
 */
public class aVisitor511 {
}
