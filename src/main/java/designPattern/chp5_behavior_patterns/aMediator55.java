package designPattern.chp5_behavior_patterns;

/**
 * 中介者模式
 * 1 意图
 *  用一个中介对象来封装一系列的对象交互。使各对象不需要显示的引用，使耦合松散，且可以独立改变他们之间的交互。（但是对象需要
 *  有中介者的引用）
 *
 * 3 适用性
 *   一组对象以定义良好但复杂的方式进行通信，产生的相互依赖关系结构混乱切难理解
 *   一个对象引用其他很多对象并直接通信，导致对象难以复用（依赖关系过多导致其中的对象不能单独拿出来用）
 *   想定制一个分布在多个类中的行为，而又不想生成太多子类
 *
 *
 *
 *
 *
 *
 *
 *
 * @author szd1007@github.com
 * @date 2019-06-15 16:54
 */
public class aMediator55 {

}
